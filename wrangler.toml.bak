# Cloudflare Pages 配置
# 需要手动在 Cloudflare Dashboard 中添加 KV 命名空间绑定:
# Pages > foodintelai > Settings > Functions > KV Namespace Bindings
# 变量名: CHECK_DATA
# 命名空间: 在 Workers & Pages > KV 中创建

pages_build_output_dir = "."

[[kv_namespaces]]
binding = "CHECK_DATA"
id = ""  # 创建KV命名空间后填入ID
